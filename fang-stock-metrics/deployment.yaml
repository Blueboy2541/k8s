apiVersion: apps/v1
kind: Deployment
metadata:
  name: fang-stock-metrics
  namespace: default
  labels:
    app: fang-stock-metrics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fang-stock-metrics
  template:
    metadata:
      labels:
        app: fang-stock-metrics
    spec:
      containers:
        - name: fang-stock-metrics
          # Built via kaniko-build-job.yaml, pushed to ttl.sh (anonymous, ephemeral registry).
          # Tag expires 24h after each push - rerun the build job to refresh it.
          image: ttl.sh/bardiyashojaei-fang-stock-metrics:24h
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: metrics
          env:
            - name: POLL_INTERVAL_SECONDS
              value: "60"
            - name: FINNHUB_API_KEY
              valueFrom:
                secretKeyRef:
                  name: finnhub-credentials
                  key: api-key
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 5
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 15
